#include <stdio.h>
#include <stdlib.h>

#include "Histogram.h"
#include "File_IO.h"
#include "Terminal_IO.h"

int main(void) {
	const int myRank = 0;
	const int numRank = 8;
	
	/*Histogram *h = NULL;
	int size = 0;
	
	h = readFile(myRank, numRank, h, &size);
	printHistogramArray(h, size);*/

	return EXIT_SUCCESS;
}
